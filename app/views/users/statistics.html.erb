

<div align="center" width="100%" style="display: inline-block;">
<div id='ympchart' width="45%" style="float: left">
	</div>
<div id='wdaybreakdown' width="45%" style="float: right"> </div>
Average Speeds for the Year (mph)
<input id="b1" type="button" value="Change Value">
<div id="avgmph" align="center"></div>


<%= render_chart(@wdaybreakdown, 'wdaybreakdown') %>
	<%= render_chart(@mileage_breakdown_year, 'ympchart') %>
	<!--< #render_chart(@average_mph, 'avgmph') %>-->
</div>
	<!--<div id='ysatlchart' style='width: 660px; height: 240px;'></div>-->
<!--< #render_chart(@speed_annotated_timeline, 'ysatlchart') %> -->
<br />
<h2>More coming soon!</h2>

<div>
<script type="text/javascript">
   google.load('visualization', '1', {packages:['gauge']});
      google.setOnLoadCallback(init);
	  var chart;
	  var data;
	  var options;
	  var button = document.getElementById('b1');
      function init() {
        data = new google.visualization.DataTable();
        data.addColumn('string', 'Label');
        data.addColumn('number', 'Value');
        data.addRows([
          ['Memory', 80]
        ]);

        options = {
          width: 400, height: 120,
          redFrom: 90, redTo: 100,
          yellowFrom:75, yellowTo: 90,
          minorTicks: 5, animation: { duration: 2000, easing: 'out' }
        };

        chart = new google.visualization.Gauge(document.getElementById('avgmph'));
        chart.draw(data, options);
      }
	  
	  function drawChart() {
	    button.disabled = true;
		google.visualization.events.addListener(chart, 'ready', 
		  function() {
		    button.disabled = false;
		  });
		  chart.draw(data, options);
	  }
	  
	  button.onclick = function() {
	    var newValue = 100 - data.getValue(0, 1);
		data.setValue(0, 1, newValue);
		drawChart();
	  }
	  
</script>
</div>